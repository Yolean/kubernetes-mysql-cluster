apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ystack-mariadb-galera
spec:
  template:
    spec:
      containers:
      - name: "mariadb-galera"
        env:
        - name: MARIADB_GALERA_FORCE_SAFETOBOOTSTRAP
          value: "yes"
